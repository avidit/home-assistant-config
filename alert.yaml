front_door:
  title: Door open !
  name: Front door has been open for {{ relative_time(states.sensor.front_door_status.last_changed) }}
  done_message: Front door is closed
  entity_id: sensor.front_door_status
  state: 'open'
  repeat:
    - 5
    - 10
    - 50
  can_acknowledge: true
  skip_first: true
  notifiers:
    - html5_moto_g6

back_door:
  title: Door open !
  name: Back door has been open for {{ relative_time(states.sensor.back_door_status.last_changed) }}
  done_message: Back door is closed
  entity_id: sensor.back_door_status
  state: 'open'
  repeat:
    - 5
    - 10
    - 50
  can_acknowledge: true
  skip_first: true
  notifiers:
    - html5_moto_g6

slider:
  title: Door open !
  name: Slider has been open for {{ relative_time(states.sensor.slider_status.last_changed) }}
  done_message: Slider is closed
  entity_id: sensor.slider_status
  state: 'open'
  repeat:
    - 5
    - 10
    - 50
  can_acknowledge: true
  skip_first: true
  notifiers:
    - html5_moto_g6
